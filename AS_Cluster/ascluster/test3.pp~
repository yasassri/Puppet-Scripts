$no_of_managers = 1
$array = ['One','two','Three']


define loop() {

notice("Configuring The Manager Nodes!!")

if ($name == $no_of_managers+1) {
    notice("Loop Iteration Finished!!!")
  }
  else
  {
    notice("Iteration Number : $name \n")
    
    # Doing All Stuff Here 
   
#resource {$array:}
    
    $next = $name + 1
    $var3 = inline_template("<%= @next.to_i*6 %>")
    
    loop2 {$var3:}
    loop { $next: }
 }
 }

loop { "1":}

define loop2() {
	
	$var = inline_template("<%= 6+@var3.to_i %>")
	#notice ("The Var = ${var}")
	$var4 = inline_template("<%= @var.to_i - @name.to_i - 1 %>")
	notice("Loop 222222 : ${var4} ")
	$next2 = $name + 1
	if($next2 == $var){}
	else{
		loop2 { $next2: }
	}
	
	#$var2 = inline_template("<%= @next.to_i+1 %>")
	
    	
	
}
