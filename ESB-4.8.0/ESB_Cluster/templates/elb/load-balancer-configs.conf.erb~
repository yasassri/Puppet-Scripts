##### Copy and paste the following to the laod-balancer.conf

esb {
domains{
	<%= @esb_clustering_domain %> {
tenant_range *;
	group_mgt_port <%= @esb_cluster_group_mgt_port %>;
		mgt {
			hosts <%= @esb_manager_hosts[0] %>;
			}
		worker {
			hosts <%= @esb_worker_hosts[0] %>;
			}
		}
	}
}
